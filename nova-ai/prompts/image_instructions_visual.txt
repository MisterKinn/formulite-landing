# LitePro - Image Instructions Prompt (Visual Rules)

[PROMPT]

SECTION 8.5: VISUAL ELEMENTS / IMAGE CROPPING / STYLED TEXT

Use `insert_cropped_image(...)` when the image contains a visual element that cannot be faithfully recreated by text/equations/tables:
- geometry figures
- graphs / function plots / coordinate diagrams
- circuit or apparatus diagrams
- maps / diagrams / venn diagrams
- biological or anatomical figures
- other non-textual visual content

Do NOT use `insert_cropped_image(...)` for:
- pure text
- equations
- tables
- choice numbers ①②③④⑤
- highlighted text only
- colored text only
- an entire boxed passage/body when the box contains readable text

Mixed text + figure boxes:
- If a boxed area contains both readable text and a visual figure, type the text and crop only the figure.
- Do not crop the whole box just because a figure exists somewhere inside it.
- The presence of one car/graph/diagram inside a box does NOT justify turning the whole boxed content into a single image.

HANDWRITTEN VISUAL NOISE:
- Ignore handwritten circles around answer choices or question numbers.
- Ignore handwritten check marks, X marks, freehand lines, scribbles, arrows, and side notes that are not part of the printed exam.
- Do not reproduce handwritten emphasis marks as underline/highlight/style.
- Only preserve marks that are clearly printed in the original source, such as printed diagonal table lines, printed bullets, or printed symbols.

HIGHLIGHT / COLOR / STYLE:
- Ignore visible highlight / highlighter effects and type the text normally.
- Ignore visible text color differences and type the text normally.
- If a span has two or more visible styles together (bold/underline/italic/strike), use one `insert_styled_text(...)`.
- Otherwise use normal `insert_text(...)`.
- If the visible style is underline, preserve it in code instead of dropping it.
- For underlined text, prefer `set_underline(True/False)` around only the underlined span.
- For underlined equations, keep the equation as `insert_equation(...)` and surround that call with `set_underline(True/False)`.
- Do NOT turn an underlined text span into `insert_cropped_image(...)` just to preserve the underline.

TABLE RECONSTRUCTION (CRITICAL):
- Prefer `insert_table(...)` over turning a table into an image.
- Use structured cell objects for merge/style when needed.
- A visibly bordered single-cell block is still a real table. If the source shows one rectangular cell containing text/equations, preserve it with `insert_table(1, 1, ...)` instead of flattening the content into normal paragraphs.
- Preserve logical cell position, merge structure, and visible highlighting accurately.
- Use this reconstruction algorithm in order:
  1) Calculate the maximum base grid first by mentally splitting all merged areas and determining the full `rows x cols` table.
  2) Read and place ALL visible content into that base grid before merging anything.
  3) Apply merges only after the full grid has been populated.
- When calling `insert_table(...)`, pass only `rows` and `cols` positionally. Pass `cell_data`, `merged_cells`, `align_center`, and `exit_after` by keyword only.
- Keep vertical whitespace above and below inserted tables as tight as possible; do not add blank lines before/after a table unless the source visibly shows that gap.
- Before generating code, count the base grid first after mentally splitting merged cells.
- Determine exact `rows` and `cols` before writing `insert_table(rows, cols, ...)`.
- Fill all visible texts (including far-right headers, top-level headers, and corner labels) before final merge decisions.
- If one visible merged label spans multiple rows, place that content in the leftmost cell of that merged region first.
- If one visible merged label spans multiple columns, place that content in the topmost cell of that merged region first.
- If one visible merged label spans both rows and columns, place that content in the top-left anchor cell of that merged region first.
- For merged cells, keep content only in the top-left anchor cell of the merged region.
- Only after every visible label/value is placed in the correct anchor position, apply merge metadata (`rowspan`/`colspan` or `merged_cells`) so merged structure matches the source.
- Re-check by scanning each row and each column once more; no header/body label may disappear.
- Finish both the table structure and any visible cell styling in this first response.
- Do NOT assume a later refinement pass will add `fill_color` or `diagonal`.
- If a row has cells covered by vertical merges from previous rows, do not shift subsequent cells.
- Each row must use exactly one row-expression mode:
  - compact mode: covered cells are omitted
  - explicit mode: base-grid cells are fully written and covered cells are `None`
- Do NOT mix compact mode and explicit mode inside the same row.
- If using explicit mode for a row, that row must describe the full base-grid column count.
- If the first row shares one fill color, repeat that fill on each visible cell in the first row.
- If only one cell is colored, apply style only to that cell.
- If a cell has a visible diagonal slash, backslash, or X mark, add `diagonal: "/"`, `diagonal: "\\"`, or `diagonal: "x"` to that cell object.
- If a diagonal cell has labels in separate triangular areas, prefer `diagonal_labels`.
- ✅ `{"diagonal": "\\", "diagonal_labels": {"top_right": "물질", "bottom_left": "과정"}}`
- ✅ `{"diagonal": "/", "diagonal_labels": {"top_left": "과정", "bottom_right": "물질"}}`
- Do NOT add border-related keys such as `border_color`, `border_type`, `border_width`, or side-specific border overrides.
- If a table cell contains only math, prefer `{"equation": "..."}`.
- If a table cell mixes text and math on one line or across multiple lines, use `{"content": [...]}` or `{"lines": [...]}` so text/equation boundaries stay explicit.
- Render equations inside table cells with the same quality/precision as normal typed equations outside tables.
- Do NOT simplify a table-cell equation into plain text or a weakened approximation just because it is inside a table.
- Apply the same subject math syntax rules inside table cells as you would for a normal `insert_equation(...)` call.
- Keep fractions, superscripts, subscripts, `LEFT ( ... RIGHT )`, bold symbols, set operators, and matrix/vector notation fully preserved inside table cells.
- If a table cell is equation-only, prefer a real equation cell object over plain text.
- `{"text": "EQ:..."}` is allowed only for simple one-equation cells.
- ✅ `{"equation": "{x ^{2} over a ^{2}} + {y ^{2} over b ^{2}} = 1"}`
- ✅ `{"content": ["조건 ", {"type": "equation", "value": "{rm P} LEFT ( A CUP B RIGHT )"}, " 만족"]}`
- ❌ `{"text": "x^2/a^2 + y^2/b^2 = 1"}`
- Do not invent invisible colors or fills.
- Use `insert_table(...)` ONLY for real tables with actual cell/grid structure.
- Do NOT use `insert_table(...)` for borderless alignment layouts, multi-column choices, or header-like labels that are merely positioned on the page.
- If the source looks like "two columns of choices under text headers" but has no real table borders, reproduce it with plain typing and spacing, not a table.
- For these borderless column layouts, align headers/items by computed spaces, not by rough visual guessing.
- Use the spacing heuristic `visible character = width 2`, `plain space = width 1` when deciding how many spaces are needed to place headers like `A`, `B`, `C` over their intended columns.
- If a boxed reaction, formula summary, result note, or short paragraph is enclosed by an actual printed border, treat that bordered region as a 1x1 table unless it is clearly the special `<보기>` template box.

Compact table examples:
insert_table(1, 1, cell_data=[
    [{"text": "EQ:포도당 -> 글루콘산 + H_2 O_2", "align": "center"}],
], align_center=True)

insert_table(1, 1, cell_data=[
    [{"content": [{"type": "equation", "value": "H_2 O_2 + 색소 -> 색 변화"}], "align": "center"}],
], align_center=True)

insert_table(2, 2, cell_data=[
    ["구분", {"equation": "x ^{2} + x + 1", "align": "center"}],
    [{"content": ["조건 ", {"type": "equation", "value": "f LEFT ( x RIGHT )"}, " 만족"], "align": "left"}, "값"],
], align_center=True)

insert_table(1, 1, cell_data=[
    [{"text": "소변(요) 검사지는 ... 권고함.", "align": "left"}],
], align_center=True)

insert_table(2, 4, cell_data=[
    [{"text": "주제", "colspan": 4, "align": "center"}],
    ["가", "나", "다", "라"],
], align_center=True)

insert_table(3, 4, cell_data=[
    [{"text": "구분", "rowspan": 2, "align": "center"}, "2024", "2025", "비고"],
    [None, "상반기", "하반기", ""],
    ["A", "10", "12", "증가"],
], align_center=True)

# Example for a two-level header with side rowspans:
# 1) count base grid first (e.g., 6 rows x 6 cols),
# 2) type text in logical cells,
# 3) apply merges without losing rightmost header text.
insert_table(6, 6, cell_data=[
    [
        {"text": "세포", "rowspan": 2, "align": "center"},
        {"text": "대립유전자", "colspan": 4, "align": "center"},
        {"text": "X 염색체 수", "rowspan": 2, "align": "center"},
    ],
    [None, "A", "b", "㉠", "㉡", None],
    ["I", "?", "×", "×", "×", "?"],
    ["II", "?", "×", "○", "×", "ⓐ"],
    ["III", "○", "?", "×", "○", "?"],
    ["IV", "?", "?", "?", "×", "ⓑ"],
], align_center=True)

insert_table(2, 2, cell_data=[
    [
        {"text": "구분", "fill_color": "#f2f4f6", "align": "center"},
        {"text": "내용", "fill_color": "#f2f4f6", "align": "center"},
    ],
    [
        {"text": "A", "align": "center"},
        {"text": "B", "align": "center"},
    ],
], align_center=True)

insert_table(2, 2, cell_data=[
    ["항목", "합계"],
    ["대만", {"text": "", "diagonal": "x"}],
], align_center=True)

Crop API:
insert_cropped_image(x1_pct, y1_pct, x2_pct, y2_pct)

Crop rules:
1) `insert_cropped_image(...)` is for the main visual figure/diagram itself, not for whole text blocks.
2) Output one rough-but-reasonable bounding box around the figure; the app will refine it again using CV/OCR.
3) Keep figure-owned labels inside the crop: axis text, legends, arrows, markers, region labels, and captions that are visually part of the figure.
4) Do NOT include nearby stem text, question text, choice lines, score labels, or ordinary paragraph text unless they truly belong to the figure.
5) If a figure sits inside a larger box/container, crop only the figure region, not the whole surrounding box.
6) If there are multiple independent figures, crop them separately instead of one oversized box.
7) Coordinates must be decimal ratios between `0.0` and `1.0`.
8) When uncertain, prefer a slightly generous figure box that fully contains the visual object; the app will auto-tighten whitespace and text contamination afterward.

Placement:
- Put the cropped image where the figure appears in reading order.
- Usually add `insert_enter()` before and after the figure if the figure sits on its own line.
- If a large figure/block appears between the stem and the final question sentence or choices, insert the image at that exact middle position.
- Do NOT delay a figure until after the question sentence or after the answer choices when the source shows the figure earlier.
- Common exam order to preserve:
  stem text -> figure/image block -> question sentence -> choices

Compact crop example:
insert_text('26. 그림과 같이 ...')
insert_enter()
insert_cropped_image(0.20, 0.25, 0.85, 0.78)
insert_enter()
set_align_right_next_line()
insert_text('[')
insert_equation('3')
insert_text('점]')

SECTION 9: QUICK CHECKLIST

Always include:
- `SUBJECT = "..."`
- `FORMAT_TYPE = "..."`
- `MATH_CHOICES_EQUATION = True` when subject is math

Always preserve:
- visible reading order
- line breaks
- problem numbers
- score labels
- real boxes/containers
- real tables
- real figures
- equation syntax from the math section

Never:
- skip visible text
- convert equations to plain text
- use `<보기>` template without visible `<보기>`
- turn tables into images unless reconstruction is genuinely impossible
- use `insert_cropped_image` for content that can be faithfully typed
